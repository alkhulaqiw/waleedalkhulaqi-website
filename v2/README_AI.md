# تعليمات أدوات الذكاء الاصطناعي (v2)

## إعداد سريع (اختبار محلي)
1. افتح الملف `v2/config.js` وأدخل مفاتيحك:
   - HUGGINGFACE_API_KEY: مفتاح Hugging Face (hf_...)
   - REPLICATE_API_KEY: (اختياري)

> تحذير: احتفظ بهذه المفاتيح سرية. إذا نويت نشر الموقع للعامة، أنصح بقوة استخدام proxy/serverless (مثلاً Vercel Serverless Function) لإخفاء المفاتيح وعدم وضعها في `config.js`.

## تشغيل محلي
- افتح `v2/index.html` أو استعمل سيرفر بسيط:
  ```bash
  cd v2
  python3 -m http.server 8000
  # ثم افتح http://localhost:8000
---

## ماذا فعلت السكربت بالضبط
1. أنشأ مجلد `v2/posts/` مع مثال مقالة Markdown وملف `index.json` لتسهيل القراءة.  
2. غيّر `v2/blog.html` ليقرأ `posts/index.json` ويعرض المقالات بصيغة HTML (باستخدام `marked` من CDN).  
3. أضاف `v2/config.js` لتمرير مفاتيح API (يمكنك تحديثه بمفاتيح Hugging Face أو Replicate).  
4. عدّل `v2/ai-tools.html` ليجري طلبات إلى Hugging Face Inference API (نص/صور) إذا وضعت مفتاحًا؛ وإلا يعمل بوضع fallback (نص تجريبي، صور من picsum).  
5. أضاف شعار `v2/logo.svg` يستخدم خط عربي هندسي (Tajawal عبر Google Fonts).  
6. أرفق ملف `v2/README_AI.md` مع تعليمات الأمان وتشغيل محلي.

---

## تعليمات سريعة بعد تنفيذ السكربت
1. **تحرير المفاتيح للاختبار**  
   - افتح `v2/config.js` وأدخل:  
     - `HUGGINGFACE_API_KEY: "hf_..."`  (مفتاحك من https://huggingface.co/settings/tokens)  
     - (اختياري) `REPLICATE_API_KEY: "..."`
2. **تشغيل محلي لاختبار**  
   ```bash
   cd ~/waleedalkhulaqi-website/v2
   python3 -m http.server 8000
   # افتح http://localhost:8000 في متصفحك
cd ~/waleedalkhulaqi-website
git checkout v2
git pull origin v2
cd ~/waleedalkhulaqi-website
nano setup_v2_ai.sh
cd ~/waleedalkhulaqi-website
nano setup_v2_ai.sh
#!/bin/bash
echo "🚀 بدء إعداد النسخة الذكية v2 لموقع وليد الخلاقي..."

# التأكد من التواجد في المسار الصحيح
cd ~/waleedalkhulaqi-website || exit

# التأكد من وجود مجلد v2
if [ ! -d "v2" ]; then
  echo "⚠️ لم يتم العثور على مجلد v2، تأكد من أنك على الفرع الصحيح!"
  exit 1
fi

cd v2

# إنشاء مجلد المقالات
mkdir -p posts
touch posts/index.json
echo "[]" > posts/index.json

# إنشاء مثال لمقال Markdown
cat > posts/مرحبا-بكم.md <<EOF
# مرحباً بكم في موقعي الجديد 🎉

هذا أول مقال في النسخة الذكية من موقعي الشخصي **وليد الخلاقي**.
يمكنك الآن قراءة المقالات، واستخدام أدوات الذكاء الاصطناعي مباشرة من الموقع.

📅 التاريخ: $(date +%Y-%m-%d)
